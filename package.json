{
  "name": "pmweather",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open --port 3000",
    "build": "vite build",
    "serve": "vite preview --host --open --port 3000"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
